1.当多线程写入时，可能会出现两个线程同时写入的情况，这个时候他们在records这个list里面写入的位置是一样的，所以会有一个线程的写入被覆盖掉，加锁就可以避免这种情况发生
2.得看情况，看cpu能同时处理多少个线程，如果cpu只能同时处理一个线程就不行了
3.主要是不希望父类被直接实例化调用方法，同时确保子类一定重载了这个方法，因为如果子类没写，就会调用父类，就会抛出这个提醒。让我们编程时避免因为子类方法未重写而导致bug